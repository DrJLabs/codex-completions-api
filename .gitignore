.env.secret
.env
.env.dev
# Any other environment variants (keep example unignored below)
.env.*
.npmrc
local-server.log
.server.pid
.server_pid
.server.log
logs/
logging/
.smoke-tool-call.log
# Dependencies
node_modules/
bmad-bak/
.bmad/
.bmad-core/
.bmad-infrastructure-devops/
.bmad-ephemeral/
.serena/
.worktrees/
external/*
!external/codex
!external/codex/
!external/codex/**
tmp/
# Playwright artifacts
test-results/*
playwright-report/
blob-report/
# BMAD (local only)
docs/private/

# Dev Codex HOME (.codev): track templates, ignore runtime artifacts below
# We re-include .codev so its example config is versioned, then selectively ignore
# transient files created during local runs.
!.codev/
!.codev/**
!.env.example

# But ignore runtime artifacts under .codev
.codev/sessions/
.codev/rollouts/
.codev/*.log
.codev/*.ndjson
.codev/auth.json

# Production Codex HOME (.codex-api): contains secrets at runtime
# Ignore everything except placeholders so the path exists after checkout.
.codex-api/*
!.codex-api/.gitkeep
!.codex-api/README.md

# Test temp artifacts
.tmp-*.ndjson
.tmp*

# Additional temp logs
.tmp-*.log
server.log
npm-debug.log*

# Coverage outputs (Vitest, others)
coverage/

# ESLint cache
.eslintcache

# Editor/OS cruft
.DS_Store
.idea/
.vscode/

# Release pointer files (host-specific paths)
releases/*.latest.path

# Ensure example env remains tracked even with broader patterns above
!.env.example

# Local helper scripts
scripts/watch-docs-sync.sh
scripts/sync-docs-to-vault.sh
bmad-bak/
.bmad/
.bmad-core/
.bmad-infrastructure-devops/
.bmad-ephemeral/
.codex/
docs/bmad/sprint-artifacts/
docs/bmad/qa/artifacts/streaming-tool-call/*
# Allow structured logging schema module under src
!src/services/logging/
!src/services/logging/**
