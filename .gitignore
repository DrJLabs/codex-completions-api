.env.secret
.env
local-server.log
.server.pid
.server.log
logs/
# Playwright artifacts
test-results/
playwright-report/
blob-report/
# BMAD (local only)
.bmad-core/
.bmad-*/

# Re-include tracked dev config and examples
!.codev/
!.codev/**
!.env.example

# But ignore runtime artifacts under .codev
.codev/sessions/
.codev/rollouts/
.codev/*.log

# Production Codex HOME (contains secrets) — ignore all except safe placeholders
.codex-api/*
!.codex-api/.gitkeep
!.codex-api/README.md
