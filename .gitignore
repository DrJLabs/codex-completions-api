.env.secret
.env
.env.dev
# Any other environment variants (keep example unignored below)
.env.*
.npmrc
local-server.log
.server.pid
.server.log
logs/
# Dependencies
node_modules/
# Playwright artifacts
test-results/
playwright-report/
blob-report/
# BMAD (local only)

# Dev Codex HOME (.codev): track templates, ignore runtime artifacts below
# We re-include .codev so its example config is versioned, then selectively ignore
# transient files created during local runs.
!.codev/
!.codev/**
!.env.example

# But ignore runtime artifacts under .codev
.codev/sessions/
.codev/rollouts/
.codev/*.log
.codev/*.ndjson
.codev/auth.json

# Production Codex HOME (.codex-api): contains secrets at runtime
# Ignore everything except placeholders so the path exists after checkout.
.codex-api/*
!.codex-api/.gitkeep
!.codex-api/README.md

# Test temp artifacts
.tmp-*.ndjson

# Additional temp logs
.tmp-*.log
server.log

# Coverage outputs (Vitest, others)
coverage/

# ESLint cache
.eslintcache

# Editor/OS cruft
.DS_Store
.idea/

# Release pointer files (host-specific paths)
releases/*.latest.path

# Ensure example env remains tracked even with broader patterns above
!.env.example
