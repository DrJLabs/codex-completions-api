PROXY_API_KEY=your-dev-key
# Optional overrides for local dev
# PORT=18000
# CODEX_HOME=.codev
#
# Production note:
#  - Provision `.codex-api/` on the host with Codex config and secrets;
#    docker-compose will mount it read-only at /app/.codex-api.
