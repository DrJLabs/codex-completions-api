Node.js ESM with modern JS (const/let, async/await). Use 2-space indentation and double quotes to match existing files. Prettier + modern ESLint flat config enforce formatting/linting. Keep routes OpenAI compatible under /v1/*, set precise Content-Type headers, and document complex logic sparingly. Traefik/Docker configs must retain required labels and ForwardAuth target (127.0.0.1:18080).